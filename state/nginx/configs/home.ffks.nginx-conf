location / {
  uwsgi_pass unix:/var/www/home.ffks/uwsgi.sock;
  include uwsgi_params;
}

location /static {
  alias /srv/http/home.ffks;
}

location /favicon.ico {
  alias /srv/http/home.ffks/favicon.ico;
}
